# AssetRipper – Quick Start

## Windows x64

1. فك ضغط `AssetRipper-Updated-win-x64.zip` كاملًا إلى مجلد جديد.
2. شغّل `AssetRipper.GUI.Free.exe` بالنقر المزدوج. ستُفتح واجهة AssetRipper في المتصفح المحلي، ولا يفترض أن تظهر نافذة Terminal.
3. استخدم أزرار **Select File** أو **Select Folder** لاختيار ملفات تملك حق تحليلها.
4. اختر مجلد تصدير منفصلًا عن مجلد اللعبة. النسخة ترفض التصدير إلى جذر القرص أو داخل المسار المستورد، وتطلب تأكيدًا قبل حذف محتوى مجلد تصدير غير فارغ.
5. إذا احتجت إلى رؤية سجل التشغيل، شغّل الملف من PowerShell أو Command Prompt مع الوسيط `--keep-console`. سيظل المتصفح المحلي هو واجهة الاستخدام.

## ملاحظات توافق

هذه النسخة تضيف دعمًا أوليًا لقراءة SerializedFile format 23 في Unity 6000.5 وما بعدها، لكنها لا تعني أن كل ملفات URP أو SerializeReference أو WebGL الحديثة مدعومة بالكامل. عند الفشل، احتفظ بملف `AssetRipper.log` وباسم إصدار Unity والمنصة وأرسل عينة تملك حق تحليلها.

لا تُشغّل ملف EXE منفردًا بعد نقله من الحزمة؛ التطبيق يحتاج إلى DLLs والملفات المرافقة الموجودة في المجلد نفسه.
